[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v
markers =
    gtk: marks tests that require GTK (deselect with '-m "not gtk"')
    slow: marks tests that are slow (deselect with '-m "not slow"')